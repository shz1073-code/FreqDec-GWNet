# 论文大纲 19.6：最终防守版

## 0. 版本定位

19.6 是在 19.5 基础上的轻量防守版，不改变模型主线，不新增大模块，只补齐一区审稿最容易追问的三个点：

1. reference frame 不能随意取第一帧。
2. 相对增量累计必须量化 drift。
3. Mamba vs GRU 不能只在固定短序列舒适区比较。

19.6 的核心主张保持不变：

> FreqDec-GWNet 用频域导丝观测、整序列低频状态学习和序列相对生理运动增量建模，实现导丝主动运动与生理背景漂移解耦，并服务无 ECG roadmapping。

## 1. 最终推荐题目

建议正式题目使用：

**Frequency-Guided Sequence-Relative Physiological Motion Decoupling for Guidewire Observation and ECG-Free Fluoroscopic Roadmapping**

中文：

**频域引导的序列相对生理运动解耦用于透视导丝观测与无 ECG 路图补偿**

理由：

- `Frequency-Guided` 保留原始频域思想。
- `Sequence-Relative` 突出 19.4/19.5 的真正创新。
- `Physiological Motion Decoupling` 指向主动/背景运动解耦。
- `Guidewire Observation and ECG-Free Roadmapping` 保留临床任务价值。
- 不把标题押在 Mamba 上，避免实验结果不够强时反噬。

模型名仍建议：

**FreqDec-GWNet**

Mamba 强结果时可在消融或变体中写：

**FreqDec-GWNet-Mamba**

## 2. 第一贡献的最终写法

### Contribution 1：Sequence-relative incremental physiological motion formulation

直接从低维生理状态回归跨患者绝对位移是一个不适定问题。相同呼吸相位在不同患者、投照角度、视野范围和呼吸深度下可能对应完全不同的图像位移，因此：

`u_t^{phys} = G(s_t)`

不适合作为主方法。

本文将无 ECG 生理运动建模重定义为序列内相对增量问题：

`Δs_t = s_t - s_{t-1}`

`Δu_t^{phys} = G_delta(s_t, Δs_t)`

`U_t = Σ_{i=r+1}^{t} Δu_i^{phys}`

其中 `r` 是选定的 reference frame，`U_t` 是当前序列内相对于 `r` 的累计生理背景位移。该 formulation 避免跨患者绝对位移回归，只学习相邻帧低频状态变化诱导的背景运动增量。

这段应放在 Introduction contribution 和 Method overview 中，而不是只放在 motion module 细节里。

## 3. 方法主体保持不变

### 3.1 Local Wire Observation Front-End

仍然是 FAST-LiteNet-derived frequency-guided guidewire observer：

- GhostEncoder
- frequency residual enhancement / local SFF / multi-scale SFF
- gated temporal fusion or feature bank
- U-Net-like decoder
- refine / segmentation / centerline heads

该分支不使用 Mamba 作为空间分割主干。

### 3.2 Global Physiological State Branch

输入：

`B_t = LP_spatial(stopgrad(F_t^{1/8}))`

`z_t^g = GAP(P_g(B_t))`

时序建模：

`s_t = StateCore(z_1^g, ..., z_t^g)`

`StateCore` 可为 GRU、Mamba、TCN。Mamba 只用于时间维度低频状态建模，不用于空间分割。

### 3.3 Sequence-Relative Motion Field

默认：

`ψ_t = [s_t, Δs_t]`

`Δu_t^{phys} = G_delta(ψ_t)`

9 维输入 `[s_{t-1}, s_t, Δs_t]` 不放主文主消融，可放附录或实现对照。4 维精简输入也作为附录消融。

## 4. Smart Reference Selection

reference frame `r` 不能默认写成 `t=0`。

推荐主策略：

1. 如果真实 clinical roadmap / mask frame 可用，直接使用临床参考帧。
2. 如果没有临床参考帧，在每段 fluoroscopy burst 的前 `N=5` 到 `10` 帧 warm-up 区间内选择质量最高的参考帧。
3. 评分可由三项组成：

`score_r = image_sharpness_r + anchor_quality_r - ||Δs_r||`

其中：

- `image_sharpness` 可用 Laplacian variance 或等价清晰度指标。
- `anchor_quality` 来自锚点质量报告。
- `||Δs_r||` 惩罚生理状态变化率大的帧。

第一帧只作为 baseline，不作为默认主策略。

论文中应强调：

> Reference selection is performed within the initial warm-up frames of each fluoroscopic burst to avoid using unstable tube-start or high-motion frames as roadmap anchors.

## 5. Drift 与 Cycle-Consistency 评估

相对增量累计天然可能漂移，因此必须在实验中量化。

### 5.1 必做指标

必须报告：

- compensation error vs time
- cumulative drift curve
- landmark residual before/after compensation

这三项是 JBHI 版本的最低要求。

### 5.2 Cycle-consistency error

如果序列中能找到近似完整呼吸周期或状态回到近似参考相位的片段，增加：

`E_cycle = ||U_K - U_0||`

其中 `K` 是回到相近低频状态的帧。理想情况下，若无明显体位或 C-arm 漂移，`E_cycle` 应接近 0。

若真实临床片段没有完整周期，则报告 near-cycle 或 phase-return 子集，不强行伪造周期。

### 5.3 Reference reset

冲 TMI 时建议加入简单的锚点触发参考重置：

当满足：

- anchor quality 高于阈值。
- 累计位移或 drift 超过预设比例。
- 当前帧 guidewire observation confidence 足够高。

可将该帧设为新的 local reference，并令后续累计从该帧重新开始。

JBHI 版本至少完成 drift curve；TMI 版本建议同时包含 cycle-consistency 或 reference reset 分析。

## 6. Mamba vs GRU 的最终定位

Mamba 不是论文根基。论文根基是 sequence-relative physiological motion decoupling。

GRU vs Mamba 必须在相同协议下比较：

- 相同 `z_t` 输入。
- 相同 state label。
- 相同 chronological/context training。
- 相同 burn-in。
- 相同 `Δu` motion field。

### 6.1 固定帧率主实验

先报告固定帧率、正常序列下的结果。这是公平 baseline。

### 6.2 变帧率 / 丢帧压力测试

增加 robustness stress test：

- random frame dropping
- downsampled fps, such as 15 fps to 7.5 fps
- irregular interval simulation

重要说明：

如果模型不显式输入 `Δt`，Mamba 和 GRU 都未必真正知道帧间时间间隔。因此变帧率实验建议至少记录或输入 `Δt`：

`z'_t = [z_t, Δt_t]`

或使用简单 time-gap embedding。

写作上不能说 “Mamba 必定碾压 GRU”。建议写：

> We further evaluate whether temporal Mamba provides additional robustness under irregular sampling and frame-drop conditions.

只有当实验显著时，才在标题或贡献中强调 Mamba。

## 7. L_motion 的最终表述

主配置：

`detach_prev_feature = true`

`detach_curr_feature = true`

`detach_state_input = false`

因此 `L_motion` 的主作用应写成：

> to optimize the state-conditioned motion mapping so that compensated background features become consistent.

不要写成：

> to directly improve the encoder feature representation.

如果做 `detach_prev_feature=false`，则可作为 feature-adaptive alignment 消融，而非主配置。

## 8. 消融矩阵最终排序

### 主文主消融

1. absolute displacement vs sequence-relative increment。
2. random zero-init vs context-prefix vs chronological carry-over。
3. double-detach motion mapping vs feature-adaptive motion loss。
4. reference frame selection。
5. fixed fps vs dropped/irregular frames for GRU vs Mamba。

### 附录或次级消融

1. 9D vs 6D vs 4D motion input。
2. no frequency enhancement。
3. global FFT vs local SFF vs multi-scale SFF。
4. with/without LP global state input。
5. max_shift sensitivity。
6. motion feature source。

这样主文消融只验证核心原则，附录消融验证工程选择。

## 9. 最终投稿判断

JBHI：

完成 segmentation、jitter、state quality、drift curve 和 application-level compensation，即可形成稳健投稿。

TMI：

还需要：

- absolute vs relative 的跨患者优势。
- cycle-consistency 或 reference reset 支撑累计稳定性。
- 真实 landmark/roadmap compensation error 显著下降。
- Mamba 在变帧率/丢帧 stress test 中有明确增益，或不把 Mamba 写进标题。

## 10. 19.6 一句话

> 19.6 不再改模型大方向，只把一区审稿最容易攻击的 reference frame、cumulative drift 和 Mamba fairness 三件事提前布防；论文主张仍是频域导丝观测 + 整序列低频状态 + 序列相对生理运动增量 + 主动/背景运动解耦。
