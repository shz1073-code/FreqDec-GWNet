# FreqDec-GWNet runtime + dev dependencies.
#
# Pin lower bounds rather than exact versions: the container ships with
# torch 2.5.x + CUDA 12.1; CPUs/CIs may pull whatever resolves consistently.
# Use a domestic mirror in mainland China:
#   pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
#
# Heavyweight runtime
torch>=2.4
numpy>=1.26
scipy>=1.13
scikit-image>=0.24
opencv-python>=4.10
Pillow>=10.0
imageio>=2.34
matplotlib>=3.9

# Configuration / IO
PyYAML>=6.0

# Test
pytest>=8.0
